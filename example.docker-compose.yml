services:
  converter:
    image: ipinfo:latest
    build:
      context: .
    volumes:
      - ./dist:/app/dist
    environment:
      IP2LOCATION_TOKEN: ${IP2LOCATION_TOKEN:?set IP2LOCATION_TOKEN in your environment}
